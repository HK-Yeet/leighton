import { HumanizeDurationOptions, Language } from "./humanize-duration.interface";
import { HumanizeDurationLanguage } from "./humanize-duration.lang";
export declare class HumanizeDuration {
    private languageUtil;
    defaultOptions: HumanizeDurationOptions;
    private options;
    constructor(languageUtil: HumanizeDurationLanguage);
    humanize(value: number, tempOptions?: HumanizeDurationOptions): string;
    setOptions(passedOptions: HumanizeDurationOptions): void;
    getSupportedLanguages(): string[];
    addLanguage(key: string, lang: Language): void;
    private doHumanization;
    private render;
    private extend;
}
